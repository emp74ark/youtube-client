import { DateIndicatorPipe } from './date-indicator.pipe';

describe('DateIndicatorPipe', () => {
  it('create an instance', () => {
    const pipe = new DateIndicatorPipe();
    expect(pipe).toBeTruthy();
  });
});
